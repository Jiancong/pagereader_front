# 后端地址（浏览器直连 host，不含 /api2；需后端开启 CORS）
# 以下两套变量名任选其一，build 时都会生效（服务器可继续用 Next 时代的 NEXT_PUBLIC_*）
VITE_API_URL=https://page2.top
# NEXT_PUBLIC_API_BASE=https://page2.top

# Google OAuth Web 客户端 ID（不配则登录弹窗不显示 Google 按钮）
VITE_GOOGLE_CLIENT_ID=
# NEXT_PUBLIC_GOOGLE_CLIENT_ID=
